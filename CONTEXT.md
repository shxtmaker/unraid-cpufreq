# CPU 频率监控上下文

本项目以 Unraid 7.3.2 为验证目标，为原生 Dashboard CPU 磁贴补充实时频率信息，数据来自主机内核暴露的处理器频率接口。Unraid 7.3.0 及以上且使用 Linux 6.18.x 的版本保留兼容加载支持，但不视为逐版本验证通过。

## 处理器与频率

**逻辑处理器**：`/proc/cpuinfo` 或 sysfs 中可单独读取频率的 CPU 编号，界面使用 `CPU N` 表示。避免将超线程或混合架构中的逻辑处理器误称为物理核心。
_避免_: 物理核心、Core N

**当前频率**：某个逻辑处理器在本次采样时报告的运行频率，统一以 MHz 作为接口内部单位，界面按数值格式化为 MHz 或 GHz。
_避免_: 额定频率、基准频率

**平均频率**：本次采样中所有成功读取的逻辑处理器当前频率的算术平均值。缺失的逻辑处理器不参与计算。
_避免_: 把缺失值按 0 计算

## Dashboard 集成

**CPU 详情**：Unraid 原生 CPU 磁贴通过“Show details”展开的逻辑处理器列表。频率信息附着在现有负载行上，不创建独立页面或独立磁贴。

**总计行**：CPU 详情中的 `Overall Load` 行。平均频率显示在这一行，代表同一次采样的整体频率统计。

**同频轮询**：频率请求使用与 Unraid `update_1` CPU 负载更新相同的 5 秒节奏，不创建更高频率的独立轮询。
